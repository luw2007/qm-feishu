# Surface adapter implementation notes

- The adapter targets Node 24.18.0 locally because the workstation default Node 22 cannot satisfy the approved Node 24 engine gate.
- Shared ports use Web `ReadableStream<Uint8Array>` so surface modules remain independent of Node and vendor SDK stream types.
- Threshold defaults remain configuration fields; the 30 MB Feishu upload ceiling is a protocol limit, not an operator tuning threshold.
- The public package exports one runtime value, `startFeishuSurface`; all other public exports are TypeScript types.
- Health starts before external dependency probes: invalid configuration still fails fast, while valid processes remain live with readiness false and retry QM/Feishu connectivity.
- Runtime recovery reuses `FEISHU_DELIVERY_POLL_MS`; the removed `CORE_RETRY_COUNT` field had no consumer and would have advertised behavior the adapter did not implement.
- Approval callback continuity uses QM's persisted `PendingApprovalRecord.request`; the adapter decodes only actor, surface, delivery target, and conversation coordinates, so restart recovery needs no local state and does not expose the original message text.
- Feishu business code `99991400` is retryable even with HTTP 200/400; the pinned SDK documents it as application frequency limiting. Other nonzero business codes remain permanent unless separately documented.
- Compatibility and release CI start `yc-software/qm@7f2c916` with memory stores and run the source-auth contract without the local opt-in skip.
- Approval records in QM `7f2c916` have no status field: HTTP 200 means the record remains pending, while allow/deny deletes it and subsequent lookup returns 404. `decodeApproval` therefore maps a present wire record to `pending`; parsing a resolved status would invent an unsupported contract.
- Local release-candidate smoke used the built `dist/main.js` against pinned QM: liveness stayed 200 while synthetic Feishu credentials kept readiness at 503, metrics remained identifier-free, missing configuration failed closed, and direct Node SIGTERM exited cleanly within the configured deadline.
- Container build/inspection, the remaining dedicated-tenant live matrix, CI URLs, immutable image digest, and `v0.1.0` publication remain release gates. This workstation has working Feishu app credentials and GitHub authentication, but no container runtime, configured Git remote, or existing `luw2007/qm-feishu` GitHub repository; the live runbook remains intentionally pending rather than recording synthetic evidence.
- Live verification on 2026-08-03 confirmed direct DM, proactive principal delivery, dependency recovery, graceful SIGTERM, and transient Feishu send recovery against QM `7f2c916` and the real Feishu Bot API. The remaining user-originated matrix rows stay pending: the workstation has no active desktop session, and Feishu Web is denied by the tenant's MDM/OS conditional-access policy; bot-authored messages are not accepted as substitutes for user-originated live evidence.
- QM `7f2c916` returns a completed idempotency replay as HTTP 200 `{ status: "ok", sessionId, sourceUserSeq, sourceAssistantEntrySeq, reply }`, not a queued run. The adapter maps that narrow wire variant to `{ replayed: true }`; intake suppresses duplicate acknowledgement/cache/watcher side effects, and approval callbacks preserve success without inventing a run ID.
- Acceptance OAuth exploration created no supported user-send path. The per-app user token file was deleted. Open Platform accepted a user `im:message` deletion draft while retaining the tenant/app identity grant; `offline_access` remains an OAuth base grant. A previously created app version `1.0.6` is audited but not published, and the platform rejected hand-written release/cancel/delete requests with code `10002`; no version state change was claimed.
- A real Web Messenger group mention reached the long-connection handler on 2026-08-03 and exposed a second terminal idempotency replay shape: QM returned HTTP 200 `{ status: "silent", sessionId }`. QM's async turn path returns `queued` for new runs and exposes a terminal result only when the idempotency key resolves to an already-terminal run, so the adapter maps only `silent` with a non-empty session ID to `{ replayed: true }`; intake and approval callers suppress duplicate acknowledgements, cache writes, and watchers. Other terminal and malformed 2xx shapes remain contract errors.
- Feishu `chat.create` expects application IDs in `bot_id_list`, not bot open IDs. The acceptance app created dedicated private group `f1f983`; `chat.list`, `chat.get`, and member listing confirmed `chat_mode: "group"` and the synthetic user membership. Group history reads remain unavailable without `im:message.group_msg`, and bot-authored instruction `675fd6` is not user-originated acceptance evidence.
- Live group-mention verification passed at 2026-08-04T13:19:03Z for message `2e2c26` in verified group `f1f983`: Feishu reported a user sender and exactly one native bot mention, the adapter accepted intake and acknowledged both deliveries, and source-auth replay with the production turn mapping returned `{ replayed: true }`.
- Live unmentioned-group verification passed at 2026-08-04T13:53:07Z for standalone text `unmentioned-check-20260804` in group `f1f983`. With the declared minimal `im:message.group_at_msg:readonly` scope and no `im:message.group_msg`, Feishu filtered the message before the event source; the adapter and pinned QM contained no exact-text match, no delivery appeared, and the user received no response. No message ID was exposed, so release evidence uses `N/A`.
- Two real user messages initially attributed to file/image attempts were Feishu `post` messages (`dda1e2` at 2026-08-05T04:28:55Z and `eaa396` at 2026-08-05T04:31:02Z), not native `file` or `image` messages. User-authorized history metadata matched both creation times to the adapter's decode failures and showed receive-side post content shaped as top-level `{ title, content, content_v2 }`; the decoder had only accepted the send-side multilingual `{ post: { <lang>: ... } }` envelope. The decoder now accepts both shapes. Embedded rich-post media remains outside the independent image/file attachment path because `message_type=post` does not provide the download contract used for native resource messages.
- The acceptance app still cannot read group history (`230027`); diagnosis used a separately authorized user read and retained only UTC timestamps, message types, sender types, structural field names, and six-character message suffixes. The built adapter smoke accepted a synthetic receive-side post containing a mention and embedded image, submitted one text turn, sent one acknowledgement, published one surface event, and made no attachment download. Those rich-post events were not native attachment evidence; later DM file `576d47` and DM image `13f1af` satisfy the independent attachment rows.
- Feishu attachment messages are standalone and cannot carry an explicit bot mention. First-release inbound attachments are therefore DM-only. Group attachment intake would require broad `im:message.group_msg` access or unsafe cross-message association with a prior mention; both conflict with the explicit-mention, fail-closed contract, so the adapter keeps `im:message.group_at_msg:readonly` and does not infer attachment addressing from previous messages.
- Live generic-file acceptance passed at 2026-08-05T08:20:41Z for real user DM file `576d47`. User-authorized history identified `message_type=file`; the adapter received the same message ID, and `intake_outcome=accepted` proves the ordered resource download, bounded read, SHA-256 calculation, QM blob staging, and attachment-bearing turn submission completed. No decode, intake, acknowledgement, or send failure was observed. The later `SANDBOX_BACKEND=local` Docker-daemon failure came from QM worker execution after turn acceptance and does not invalidate attachment transfer.
- Live image acceptance passed at 2026-08-05T13:33:23Z for real user DM image `13f1af`. User-authorized history identified `message_type=image`; the adapter received the same message ID, completed the 10 MB-bounded download and QM blob staging before `intake_outcome=accepted`, and acknowledged the delivery without decode, intake, acknowledgement, or send failure. The user-visible Docker error and terminal run-failed notice were later QM worker outcomes, not image-transfer failures.
- Live stop acceptance passed at 2026-08-05T14:49:03Z for real user DM `stop` message `3ccbdd`. An isolated in-memory QM process ran with `BACKGROUND_WORK_ENABLED=false` only to keep the seed run non-terminal without Docker. Real user seed `b38a28` created the sole active run and acknowledgement `189582`; `stop` arrived under the same root/thread, the adapter returned `intake_outcome=signaled`, QM kept the same run ID with no second run, and Feishu emitted `Stopped.` receipt `58ad4f`. Feishu WebSocket intake, source-auth HTTP, and reply delivery remained live.
- Real user topic message `46e1df` at 2026-08-05T15:17:35Z verified incoming mapping only: one native bot mention, verified root `2e2c26` and thread `8f1bb7`, and accepted intake without decode, intake, or acknowledgement failure. Minimal-scope history cannot enumerate the app reply and the adapter did not retain its receipt, so `reply_in_thread=true` code semantics alone are insufficient live outbound evidence. Topic continuity remains pending until a fresh reply receipt is directly observed.
- Live absent-approval rejection passed at 2026-08-05T15:25:50Z for interactive card `a96303` and nonexistent request suffix `910058`. A real user click produced a verified Feishu callback, QM returned no approval, the adapter recorded `approval_action_outcome=missing`, and Feishu displayed `This approval request could not be found.` Repeated clicks remained missing; no continuation or active DM run appeared.
- Live allow-once acceptance passed at 2026-08-06T03:04:45Z for card `723c58` and authoritative QM request `d85f58`. Pinned QM's deterministic mock harness created the real pending record through source-auth without Docker; its requester matched the real synthetic Feishu operator and granted `once`. The verified callback reloaded the record, submitted a scope-`once` continuation, and returned `approval_action_outcome=accepted`; QM then reported the record missing, and a repeated click remained missing.
- Live deny acceptance passed at 2026-08-06T03:26:12Z for card `770fcd` and a separate authoritative request `748399`. The verified callback submitted `approved=false` with no scope and returned `approval_action_outcome=denied`; the QM record disappeared, no active run remained, and repeated clicks returned missing.
- Live ordinary-follow-up verification found a pinned-QM contract mismatch. Real user seed `8e6eac` and follow-up `84bbf8` created separate pending runs because qm-feishu correctly supplies deterministic per-message idempotency keys, while QM `7f2c916` only folds a follow-up into an active run when `req.idempotencyKey` is absent. A source-auth differential reproduced keyed `sameRun=false`/`steered=false` versus unkeyed `sameRun=true`/`steered=true`. QM's signal endpoint has no idempotency key, so deleting the turn key or signaling directly would make Feishu replays inject duplicate steers. The live steer row remains blocked pending an atomic upstream contract.
